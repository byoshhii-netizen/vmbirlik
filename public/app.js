const $ = (s, root=document) => root.querySelector(s);
const esc = (v='') => String(v).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const api = async (url, options={}) => { const r=await fetch(url,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options}); const data=await r.json().catch(()=>({})); if(!r.ok) throw new Error(data.error||'İşlem gerçekleştirilemedi.'); return data; };
let site={settings:{},announcements:[],team:[]};
const nav = () => `<header class="topbar"><a class="brand" href="/"><img src="/assets/vmbirlik.png" alt="VMB"><span>Vecd ile<br>Müdafaa Birliği</span></a><button class="menu-toggle" aria-label="Menüyü aç" aria-expanded="false"><i></i><i></i></button><nav class="nav"><span class="menu-label">VMB / MENÜ</span><a href="/arsiv">Arşiv</a><a href="/ekip">Ekip</a><a href="/duyurular">Duyurular</a><a href="/hakkimizda">Hakkımızda</a><a href="/iletisim">İletişim</a></nav></header>`;
const footer = () => `<footer class="footer"><div><h2>Ortak hafıza,<br>müşterek istikamet.</h2><p>© ${new Date().getFullYear()} Vecd ile Müdafaa Birliği</p></div><a class="outline-button" href="/arsiv">Arşivi incele</a></footer>`;
const label = v => ({public:'Açık arşiv',private:'Kişisel arşiv',admin:'Yönetim arşivi',password:'Korumalı arşiv',pending:'İnceleme bekliyor',hidden:'Gizli'}[v]||v);
async function load(){ site=await api('/api/site'); }
function announcementLayer(){ const layer=$('#notice-layer'); const toast=site.announcements.find(a=>a.type==='toast'); const modal=site.announcements.find(a=>a.type==='modal'); layer.innerHTML=(toast?`<aside class="toast" style="border-color:${esc(toast.color)}"><button aria-label="Kapat">×</button><h3>${esc(toast.title)}</h3><p>${esc(toast.body)}</p></aside>`:'')+(modal?`<section class="modal"><article class="modal-box"><button class="modal-close" aria-label="Kapat">×</button>${modal.image_url?`<img style="width:100%;max-height:260px;object-fit:cover;margin-bottom:20px" src="${esc(modal.image_url)}" alt="">`:''}<span class="index">DUYURU</span><h2>${esc(modal.title)}</h2><p>${esc(modal.body)}</p><button class="button">Tamam</button></article></section>`:''); layer.querySelectorAll('button').forEach(b=>b.onclick=()=>layer.innerHTML=''); }
async function home(){const b=site.settings.brand||{}; const list=(await api('/api/archives')).archives.filter(a=>a.visibility==='public').slice(0,3); $('#app').innerHTML=`${nav()}<main><section class="hero"><div class="hero-copy"><span class="index">${esc(b.eyebrow||'VMB / 01')}</span><h1>${esc(b.heroTitle||'')}</h1><p>${esc(b.heroText||'')}</p><a class="button" href="/arsiv">Arşivi keşfet</a></div><div class="hero-side"><span class="index">BİRLİK NOTU</span><p>${esc(b.quote||'')}</p></div><span class="scroll-mark">AŞAĞI KAYDIR / 01</span></section><section class="section"><div class="section-heading"><span class="index">01 / İÇERİKLER</span><div><h2>Kayda değer olanı saklamak.</h2><p>Metinler, görsel belgeler, bağlantılar ve kayıtlar; bağlamlarını koruyacak bir düzen içinde bir araya gelir.</p></div></div><div class="archive-grid">${list.length?list.map((a,i)=>`<a class="archive-card" href="/arsiv/${a.id}"><span class="card-number">0${i+1} / ${label(a.visibility)}</span><h3>${esc(a.title)}</h3><p>${esc(a.description||'Açıklama henüz eklenmedi.')}</p></a>`).join(''):`<article class="archive-card"><span class="card-number">01 / BAŞLANGIÇ</span><h3>Arşiv hazırlanıyor.</h3><p>İlk kayıtlar yakında burada yer alacak.</p></article>`}</div></section><section class="statement"><blockquote>“${esc(b.quote||'Birlik, aynı sesi yükseltmek değil; aynı sorumluluğu taşımaktır.')}”</blockquote><cite>VECD İLE MÜDAFAA BİRLİĞİ / İLKE NOTU</cite></section><section class="section"><div class="section-heading"><span class="index">02 / BİRLİK</span><div><h2>Bir araya gelen emek.</h2><p>Çalışmalar, paylaşımlar ve yeni arşiv talepleri için ortak bir alan.</p></div></div><a class="outline-button" href="/arsiv">Arşiv oluşturma alanına git</a></section></main>${footer()}`;}
async function archives(){const data=await api('/api/archives'); $('#app').innerHTML=`${nav()}<main class="page"><section class="page-hero"><span class="index">VMB / ARŞİV</span><h1>Kayda geçenler.</h1><p>Her arşiv ilk aşamada yalnızca sahibine görünür. İnceleme ardından herkese açılabilir; kişi kendi kaydını daha sonra yeniden gizleyebilir.</p></section><section class="archive-layout"><div class="archive-list">${data.archives.map(a=>`<a class="archive-row" href="/arsiv/${a.id}"><div><span class="tag">${label(a.visibility)}${a.status==='pending'?' / inceleme bekliyor':''}</span><h2>${esc(a.title)}</h2><p>${esc(a.description)}</p></div><span class="card-number">${new Date(a.created_at).toLocaleDateString('tr-TR')}</span></a>`).join('')||'<div class="archive-row"><p>Henüz görünür bir arşiv yok.</p></div>'}</div><aside class="archive-aside"><span class="index">YENİ KAYIT</span><h3>Arşiv oluştur</h3><p>Talebiniz başlangıçta gizlidir. Onaylandığında herkese açık hale gelir.</p><form id="archive-form" class="form"><input name="title" maxlength="140" placeholder="Arşiv başlığı" required><textarea name="description" placeholder="Kısa açıklama"></textarea><button class="button">Talep oluştur</button></form></aside></section></main>${footer()}`; $('#archive-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);try{await api('/api/archives',{method:'POST',body:JSON.stringify(Object.fromEntries(f))}); alert('Arşiv talebiniz oluşturuldu. Şu anda yalnızca siz görebilirsiniz.'); location.reload()}catch(x){alert(x.message)}};}
async function archiveDetail(id){const d=await api('/api/archives/'+id); const a=d.archive; $('#app').innerHTML=`${nav()}<main class="page"><section class="page-hero"><span class="index">ARŞİV / ${label(a.visibility)}</span><h1>${esc(a.title)}</h1><p>${esc(a.description)}</p>${a.owner?`<button id="toggle-archive" class="outline-button">${a.visibility==='public'?'Arşivi gizle':'Onay için aç'}</button>`:''}</section><section class="detail-body"><span class="index">İÇERİK / ${d.items.length} KAYIT</span><div class="file-grid">${d.items.map(x=>`<article class="file-card"><span class="tag">${esc(x.kind)}</span><h3>${esc(x.title)}</h3>${x.body?`<p>${esc(x.body)}</p>`:''}${x.media_url?`<a target="_blank" href="${esc(x.media_url)}">İçeriği aç</a>`:''}</article>`).join('')||'<article class="file-card"><h3>İçerik henüz eklenmedi.</h3></article>'}</div>${a.owner?`<aside class="archive-aside" style="margin-top:28px"><span class="index">YENİ İÇERİK</span><h3>Arşive ekle</h3><form id="item-form" class="form"><select name="kind"><option value="text">Metin</option><option value="link">Bağlantı</option><option value="image">Fotoğraf</option><option value="video">Video</option><option value="folder">Klasör</option></select><input name="title" placeholder="Başlık" required><textarea name="body" placeholder="Metin veya bağlantı açıklaması"></textarea><input name="file" type="file"><button class="button">Kaydet</button></form></aside>`:''}</section></main>${footer()}`; const toggle=$('#toggle-archive');if(toggle)toggle.onclick=async()=>{try{await api('/api/archives/'+id+'/visibility',{method:'PATCH',body:JSON.stringify({visibility:a.visibility==='public'?'private':'public'})});location.reload()}catch(e){alert(e.message)}}; const form=$('#item-form');if(form)form.onsubmit=async e=>{e.preventDefault();const r=await fetch('/api/archives/'+id+'/items',{method:'POST',body:new FormData(form)});const x=await r.json().catch(()=>({}));if(!r.ok)return alert(x.error||'Kaydedilemedi.');location.reload()};}
function simplePage(key,title,copy,content){ $('#app').innerHTML=`${nav()}<main class="page"><section class="page-hero"><span class="index">VMB / ${key}</span><h1>${title}</h1><p>${copy}</p></section>${content}</main>${footer()}`; }
function team(){const cards=site.team.map(x=>`<article class="team-card"><span class="card-number">${esc(x.role||'EKİP')}</span><h3>${esc(x.name)}</h3><p>${esc(x.bio||'')}</p></article>`).join('')||'<article class="team-card"><h3>Ekip bilgileri yakında burada.</h3></article>';simplePage('EKİP','Birlikte çalışanlar.','Farklı yeteneklerin ortak iradeye dönüştüğü ekip.',`<section class="section"><div class="team-grid">${cards}</div></section>`)}
function about(){const a=site.settings.about||{};simplePage('HAKKIMIZDA',esc(a.title||'Hakkımızda'),esc(a.body||''),`<section class="statement"><blockquote>${esc(a.body||'')}</blockquote></section>`)}
function announcements(){const articles=site.announcements.filter(x=>x.type==='article');simplePage('DUYURULAR','Güncel notlar.','Birliğin güncel paylaşımları ve açıklamaları.',`<section class="section"><div class="announcement-grid">${articles.map(x=>`<article class="announcement-card"><span class="card-number">${new Date(x.created_at).toLocaleDateString('tr-TR')} / ${esc(x.author||'VMB')}</span>${x.image_url?`<img src="${esc(x.image_url)}" alt="" style="width:100%;height:150px;object-fit:cover;margin-top:18px">`:''}<h3>${esc(x.title)}</h3><p>${esc(x.body)}</p></article>`).join('')||'<article class="announcement-card"><h3>Henüz duyuru yok.</h3></article>'}</div></section>`)}
function contact(){const c=site.settings.contact||{};const links=(c.links||[]).filter(x=>x.active).map(x=>`<a href="${esc(x.url||'#')}" target="_blank">${esc(x.label)}</a>`).join('');simplePage('İLETİŞİM',esc(c.title||'İletişim'),'Aktif kanallar üzerinden bizimle bağlantı kurabilirsiniz.',`<section class="section"><div class="socials">${links||'<p>İletişim kanalları yakında eklenecek.</p>'}</div></section>`)}
async function admin(){ $('#notice-layer').innerHTML=''; const logged=await fetch('/api/admin/dashboard').then(r=>r.ok?r.json():null); if(!logged){$('#app').innerHTML=`<main class="login"><section class="modal-box"><span class="index">VMB / YÖNETİM</span><h2>Yetkili erişim</h2><p>Yönetim alanı, yalnızca yetkili kullanıcılar içindir.</p><form id="login-form" class="form"><input name="username" placeholder="Kullanıcı adı" value="yonetici"><input name="password" type="password" placeholder="Parola" required><button class="button">Oturum aç</button></form></section></main>`;$('#login-form').onsubmit=async e=>{e.preventDefault();try{await api('/api/auth/login',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});admin()}catch(x){alert(x.message)}};return} const render=async tab=>{const el=$('.admin-content'); if(tab==='overview'){el.innerHTML=`<div class="admin-grid">${[['Arşivler',logged.archives],['İnceleme bekleyen',logged.pending],['Arşivci',logged.creators],['Güvenlik kaydı',logged.security]].map(x=>`<article class="admin-stat"><b>${x[1]}</b><span>${x[0].toUpperCase()}</span></article>`).join('')}</div>`;return} if(tab==='content'){const s=await api('/api/admin/settings');el.innerHTML=`<span class="index">TASARIM VE METİNLER</span><p style="color:var(--muted);line-height:1.7">Ana sayfa, hakkımızda ve iletişim alanlarının JSON tabanlı ayarları. Bu alan yöneticinin tüm başlık ve metinleri değiştirmesini sağlar.</p><form id="setting-form" class="form"><select name="key">${s.map(x=>`<option value="${x.key}">${x.key}</option>`).join('')}</select><textarea name="value" placeholder="JSON düzenlemesi"></textarea><button class="button">Kaydet</button></form>`;$('#setting-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);try{await api('/api/admin/settings/'+f.get('key'),{method:'PUT',body:f.get('value')});alert('Kaydedildi.')}catch(x){alert('Geçerli JSON girin.')}};return} if(tab==='announcements'||tab==='team'){const isAnn=tab==='announcements';const rows=await api('/api/admin/'+tab);el.innerHTML=`<span class="index">${isAnn?'DUYURU EKLE':'EKİP ÜYESİ EKLE'}</span><form id="manage-form" class="form"><input name="${isAnn?'title':'name'}" placeholder="${isAnn?'Başlık':'Ad soyad'}" required>${isAnn?'<select name="type"><option value="toast">Sağ alt bildirim</option><option value="modal">Merkez duyuru</option><option value="article">Duyuru sayfası</option></select><textarea name="body" placeholder="Duyuru metni" required></textarea><input name="author" placeholder="Yazan kişi"><input name="color" value="#b89657" placeholder="Vurgu rengi">':'<input name="role" placeholder="Rol"><textarea name="bio" placeholder="Kısa biyografi"></textarea>'}<button class="button">Kaydet</button></form><div style="overflow:auto;margin-top:28px"><table class="data-table"><thead><tr><th>Başlık / ad</th><th>Tür / rol</th><th>Durum</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${esc(r.title||r.name)}</td><td>${esc(r.type||r.role||'—')}</td><td>${r.active===false||r.visible===false?'Gizli':'Görünür'}</td></tr>`).join('')}</tbody></table></div>`;$('#manage-form').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));try{await api('/api/admin/'+tab,{method:'POST',body:JSON.stringify(d)});render(tab)}catch(x){alert(x.message)}};return} const rows=await api('/api/admin/'+tab);const fields=tab==='archives'?['title','visibility','status','created_at']:tab==='security'?['ip_address','route','kind','created_at']:tab==='creators'?['ip_address','archive_count','archive_reads','created_at','last_archive_at']:tab==='bans'?['ip_address','scope','active','created_at']:['name','role','visible'];el.innerHTML=`<div style="overflow:auto"><table class="data-table"><thead><tr>${fields.map(f=>`<th>${f}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${fields.map(f=>`<td>${esc(typeof r[f]==='object'?JSON.stringify(r[f]):r[f]??'—')}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;}; $('#app').innerHTML=`<main class="admin"><header class="admin-header"><div><span class="index">VMB / YÖNETİM</span><h1>Kontrol merkezi.</h1></div><button id="logout" class="outline-button">Çıkış</button></header><nav class="admin-tabs">${[['overview','Genel bakış'],['archives','Arşivler'],['creators','Arşivciler'],['announcements','Duyurular'],['team','Ekip'],['content','Tasarım ve metin'],['security','Erişim kayıtları'],['bans','Erişim kısıtları']].map((x,i)=>`<button class="${i?'':'active'}" data-tab="${x[0]}">${x[1]}</button>`).join('')}</nav><section class="admin-content"></section></main>`;$('.admin-tabs').onclick=e=>{if(e.target.dataset.tab){$$('.admin-tabs button').forEach(x=>x.classList.remove('active'));e.target.classList.add('active');render(e.target.dataset.tab)}};$('#logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'});admin()};render('overview'); }
async function adminV2(){const dash=await fetch('/api/admin/dashboard').then(r=>r.ok?r.json():null);if(!dash){$('#app').innerHTML=`<main class="login"><section class="admin-login"><span class="index">VMB / YÖNETİM</span><h1>Yönetim girişi</h1><p>İçerik, duyuru ve arşiv yönetimi için oturum aç.</p><form id="v2-login" class="form"><input name="username" value="yonetici" placeholder="Kullanıcı adı"><input name="password" type="password" placeholder="Parola" required><button class="button">Giriş yap</button></form></section></main>`;$('#v2-login').onsubmit=async e=>{e.preventDefault();try{await api('/api/auth/login',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});adminV2()}catch(x){alert(x.message)}};return}const view=async name=>{const panel=$('#v2-panel');if(name==='home'){panel.innerHTML=`<div class="admin-welcome"><span class="index">GENEL BAKIŞ</span><h2>Her şey burada.</h2><p>Yeni duyuru yayınla, ekip üyesi ekle veya site metnini güncelle.</p></div><div class="admin-grid v2-grid">${[['Toplam arşiv',dash.archives],['Onay bekleyen',dash.pending],['Arşivci',dash.creators],['Erişim kaydı',dash.security]].map(x=>`<article class="admin-stat"><b>${x[1]}</b><span>${x[0]}</span></article>`).join('')}</div>`;return}if(name==='site'){const all=await api('/api/admin/settings');const brand=all.find(x=>x.key==='brand')?.value||{};panel.innerHTML=`<div class="panel-title"><span class="index">SİTE METİNLERİ</span><h2>Ana sayfayı düzenle</h2></div><form id="brand-form" class="easy-form"><label>Üst küçük başlık<input name="eyebrow" value="${esc(brand.eyebrow||'')}"></label><label>Ana başlık<textarea name="heroTitle">${esc(brand.heroTitle||'')}</textarea></label><label>Açıklama<textarea name="heroText">${esc(brand.heroText||'')}</textarea></label><label>Vurgu sözü<textarea name="quote">${esc(brand.quote||'')}</textarea></label><button class="button">Değişiklikleri kaydet</button></form>`;$('#brand-form').onsubmit=async e=>{e.preventDefault();await api('/api/admin/settings/brand',{method:'PUT',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});alert('Ana sayfa metinleri kaydedildi.');};return}if(name==='announcement'||name==='team'){const ann=name==='announcement';const rows=await api('/api/admin/'+(ann?'announcements':'team'));panel.innerHTML=`<div class="panel-title"><span class="index">${ann?'DUYURU':'EKİP'}</span><h2>${ann?'Yeni duyuru oluştur':'Ekip üyesi ekle'}</h2></div><form id="easy-create" class="easy-form"><label>${ann?'Başlık':'Ad soyad'}<input name="${ann?'title':'name'}" required></label>${ann?`<label>Tür<select name="type"><option value="toast">Sağ alt bildirim</option><option value="modal">Ekran ortası duyuru</option><option value="article">Duyurular sayfası</option></select></label><label>Metin<textarea name="body" required></textarea></label><label>Yazan kişi<input name="author"></label>`:`<label>Rol<input name="role"></label><label>Tanıtım metni<textarea name="bio"></textarea></label>`}<button class="button">Yayınla</button></form><h3 class="list-heading">Mevcut kayıtlar</h3><div class="simple-list">${rows.length?rows.map(x=>`<article><b>${esc(x.title||x.name)}</b><span>${esc(x.type||x.role||'')}</span></article>`).join(''):'<p>Henüz kayıt yok.</p>'}</div>`;$('#easy-create').onsubmit=async e=>{e.preventDefault();await api('/api/admin/'+(ann?'announcements':'team'),{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});view(name)};return}const type=name==='archives'?'archives':name==='creators'?'creators':'security';const rows=await api('/api/admin/'+type);panel.innerHTML=`<div class="panel-title"><span class="index">${name.toUpperCase()}</span><h2>${name==='archives'?'Arşivler':name==='creators'?'Arşivciler':'Erişim kayıtları'}</h2></div><div class="simple-list">${rows.length?rows.map(x=>`<article><b>${esc(x.title||x.ip_address||'Kayıt')}</b><span>${esc(x.status||x.route||x.created_at||'')}</span></article>`).join(''):'<p>Henüz kayıt yok. Yeni arşiv veya duyuru eklediğinde burada görünecek.</p>'}</div>`};$('#app').innerHTML=`<main class="admin-v2"><aside class="admin-side"><div><span class="index">VMB / YÖNETİM</span><h1>Yönetim</h1></div><nav id="v2-nav"><button data-view="home" class="active">Genel bakış</button><button data-view="site">Site metinleri</button><button data-view="announcement">Duyurular</button><button data-view="team">Ekip</button><button data-view="archives">Arşivler</button><button data-view="creators">Arşivciler</button><button data-view="security">Erişim kayıtları</button></nav><button id="v2-logout">Oturumu kapat</button></aside><section id="v2-panel" class="admin-main"></section></main>`;$('#v2-nav').onclick=e=>{if(e.target.dataset.view){$$('#v2-nav button').forEach(x=>x.classList.remove('active'));e.target.classList.add('active');view(e.target.dataset.view)}};$('#v2-logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'});adminV2()};view('home')}
const $$=(s,r=document)=>[...r.querySelectorAll(s)];
async function route(){try{await load();const path=location.pathname;if(path==='/')home();else if(path==='/arsiv')archives();else if(path.startsWith('/arsiv/'))archiveDetail(path.split('/')[2]);else if(path==='/ekip')team();else if(path==='/hakkimizda')about();else if(path==='/duyurular')announcements();else if(path==='/iletisim')contact();else if(path==='/ahbiratasver')return adminV2();else home();announcementLayer()}catch(e){$('#app').innerHTML=`<main class="plain-state"><main><h1>Bir sorun oluştu.</h1><p>${esc(e.message)}</p></main></main>`}}
function enhance(){const hero=$('.hero');if(hero)hero.onpointermove=e=>{const r=hero.getBoundingClientRect();hero.style.setProperty('--hero-x',`${((e.clientX-r.left)/r.width)*100}%`);hero.style.setProperty('--hero-y',`${((e.clientY-r.top)/r.height)*100}%`)};const toggle=$('.menu-toggle'),menu=$('.nav');if(toggle&&menu)toggle.onclick=()=>{const open=menu.classList.toggle('open');toggle.classList.toggle('open',open);toggle.setAttribute('aria-expanded',open)};const reveal=$$('.section,.archive-card,.team-card,.announcement-card,.archive-row');const io=new IntersectionObserver(xs=>xs.forEach(x=>{if(x.isIntersecting){x.target.classList.add('visible');io.unobserve(x.target)}}),{threshold:.12});reveal.forEach((x,i)=>{x.classList.add('reveal');x.style.transitionDelay=`${Math.min(i%4,3)*70}ms`;io.observe(x)})}
document.addEventListener('pointermove',e=>{document.documentElement.style.setProperty('--mx',`${e.clientX}px`);document.documentElement.style.setProperty('--my',`${e.clientY}px`)});document.addEventListener('click',e=>{const a=e.target.closest('a[href^="/"]');if(a&&a.target!=='_blank'){e.preventDefault();history.pushState({},'',a.href);route()}});window.onpopstate=route;const baseRoute=route;route=async()=>{await baseRoute();enhance()};route();
